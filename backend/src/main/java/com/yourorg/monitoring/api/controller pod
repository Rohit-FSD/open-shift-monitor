@RestController
@RequiredArgsConstructor
@RequestMapping("/api/openshift")
public class OpenShiftController {

    private final OpenShiftService service;

    @GetMapping("/pods")
    public ResponseEntity<?> getPods(@RequestParam String env) {
        return ResponseEntity.ok(service.getPods(env));
    }
}
