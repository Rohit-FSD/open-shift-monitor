I am building a Spring Boot utility to dynamically monitor OpenShift environments.

Current requirement:

I have multiple environments (QA1, QA2, QA3, QA4, QA11, QA12, QA21, QA22, QA41)
Each environment belongs to a cluster (np1, np3, np5)
Each environment has a namespace
Each cluster has a different OpenShift API host
Tokens cannot be hardcoded because they expire
To ensure this utility dynamically connects to any environment without hardcoding tokens and handles token expiry correctly.
